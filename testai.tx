use "test.tx" as t;

struct Hello {
	str world;
	str getWorld(self) {
		ret self.world;
	}
}

extern {
	extern *void malloc(size size);
}

inline i32 add(i32 x, i32 y) {
	ret x + y;
}

u8 main(u32 argc, str *argv) {
	i32 x = add(3, 2);
	// wow we did something
	ret x
}
